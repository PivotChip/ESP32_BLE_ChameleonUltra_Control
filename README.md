# ESP32_BLE_ChameleonUltra_Control
Use ESP32 as remote controller for you Chameleon device. Using the NimBLE library, it manages discovery, secure pairing, and custom binary messaging. The code in Canvas uses a state machine to handle connecting and executing commands like HF/LF scanning or mode switching, parsing responses into readable logs for research.
